{% extends "template.html.twig" %}
{% block title %}{% endblock %}
{% block body %}
{% include "message.html.twig" %}
 <div class="form-group">
    <label for="exampleFormControlSelect1">Search</label>
    <select onchange="location = this.value;" class="form-control" id="exampleFormControlSelect1">
      <option value="{{path('personne.alls', {age: 30})}}"> Personnes 30ans</option>
      <option value="{{path('personne.find', {ageMin: 30, ageMax: 50,})}}"> Personnes 30ans a 50ans</option>
      <option value="{{path('personne.find', {ageMin: 30, ageMax: 40,})}}"> Personnes 30ans a 40ans</option>
      3</option>
      <option>4</option>
      <option>5</option>
    </select>
  </div>
<div class="row">
 <div class="col-12">
 
  
 

<table class="table">
  <thead>
    <tr>
      <th scope="col">📟</th>
      <th scope="col">Firstname 🔥</th>
      <th scope="col">name 👨‍🔧</th>
      <th scope="col">age 🥯</th>
            <th scope="col">kill 🤐</th>
    </tr>
  </thead>
  <tbody>
  {% for personne in personnes %}
    <tr>
      <th scope="row"><a href="{{path('personne.id', {id: personne.id}) }}">{{personne.id}}</a></th>
      <td>{{personne.firstname}}</td>
      <td>{{personne.name}}</td>
      <td>{{personne.age}}</td>
      <td><a href="{{path('personne.delete', {id: personne.id})}}"> <button type="button" class="btn btn-danger">kill</button></a></td>

    </tr>
 {% endfor %}    
  </tbody>
</table>

<div class="container mt-3">
{% if isPagination %}
<nav aria-label="Page navigation example">
  <ul class="pagination justify-content-center mt-3">
    <li class="page-item disabled">
      <a class="page-link" href="#" tabindex="-1" aria-disabled="true">Previous</a>
    </li>
    <li class="page-item"><a class="page-link" href="#">1</a></li>
    <li class="page-item"><a class="page-link" href="#">2</a></li>
    <li class="page-item"><a class="page-link" href="#">3</a></li>
    <li class="page-item">
      <a class="page-link" href="#">Next</a>
    </li>
  </ul>
</nav>
{% endif %}
</div>
 </div>
</div>
{% endblock %}

